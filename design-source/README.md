# 设计源（Open Design）

权威可操作设计图目录（本机）：

```
C:\Users\13115\AppData\Roaming\Open Design\namespaces\release-stable-win\data\projects\13c9e2bd-fdb6-4a86-b8c9-2aafa1eff47a
```

| 文件 | 说明 |
|---|---|
| `brand-spec.md` | 色板 / 文案姿态（副本） |
| `open-design-app.css` | 原型完整 CSS 快照（含手机框，仅对照） |

**实现约定**：CodeOrbit-Client 的 Vue 界面必须对齐该项目 `mobile-android/*` 与 `css/app.css` 的产品区样式；手机外框（`.device`）仅用于设计预览，不进 App。
